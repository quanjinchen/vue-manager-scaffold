export const STORAGE_KEYS = {
  token: 'scaffold.auth.token',
  profile: 'scaffold.auth.profile',
  permissions: 'scaffold.auth.permissions',
  menus: 'scaffold.auth.menus',
  settings: 'scaffold.app.settings'
} as const;
